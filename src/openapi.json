{
  "openapi": "3.0.0",
  "info": {
    "title": "Voting API",
    "version": "1.0.0",
    "description": "An API for setting up a voting system, casting votes, and retrieving voting results."
  },
  "servers": [
    {
      "url": "http://localhost:3000",
      "description": "Local server"
    }
  ],
  "paths": {
    "/setup": {
      "get": {
        "tags": [
          "Setup"
        ],
        "summary": "Initialize the database with the votes table",
        "operationId": "setupDatabase",
        "responses": {
          "200": {
            "description": "Table created successfully"
          },
          "500": {
            "description": "Server error"
          }
        }
      }
    },
    "/results": {
      "get": {
        "tags": [
          "Votes"
        ],
        "summary": "Retrieve all votes",
        "operationId": "getAllVotes",
        "responses": {
          "200": {
            "description": "A list of votes",
            "content": {
              "application/json": {
                "schema": {
                  "type": "array",
                  "items": {
                    "$ref": "#/components/schemas/Vote"
                  }
                }
              }
            }
          },
          "500": {
            "description": "Server error"
          }
        }
      }
    },
    "/cast": {
      "post": {
        "tags": [
          "Votes"
        ],
        "summary": "Cast a vote",
        "operationId": "castVote",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CastVote"
              }
            }
          }
        },
        "responses": {
          "200": {
            "description": "Vote cast successfully"
          },
          "500": {
            "description": "Server error"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "Vote": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "description": "Unique identifier for the vote"
          },
          "choice": {
            "type": "string",
            "description": "The chosen option for this vote"
          }
        }
      },
      "CastVote": {
        "type": "object",
        "properties": {
          "vote": {
            "type": "string",
            "description": "The option to cast a vote for"
          }
        },
        "required": [
          "vote"
        ]
      }
    }
  }
}